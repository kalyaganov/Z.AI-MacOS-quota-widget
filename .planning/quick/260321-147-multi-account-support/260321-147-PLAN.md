---
phase: 260321-147-multi-account-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ZaiSubscriptionWidget/Models/Account.swift
  - ZaiSubscriptionWidget/Services/KeychainService.swift
  - ZaiSubscriptionWidget/ViewModels/UsageViewModel.swift
  - ZaiSubscriptionWidget/Views/SettingsView.swift
  - ZaiSubscriptionWidget/Views/MenuBarView.swift
autonomous: true
requirements: [M2-01, M2-02, M2-03]
user_setup: []

must_haves:
  truths:
    - "User can add multiple accounts with unique names"
    - "User can switch between accounts in the menu bar"
    - "Each account stores its own API key securely"
    - "Settings shows list of all configured accounts"
    - "Active account's usage data is displayed"
  artifacts:
    - path: "ZaiSubscriptionWidget/Models/Account.swift"
      provides: "Account data model with id, name, apiKey"
    - path: "ZaiSubscriptionWidget/Services/KeychainService.swift"
      provides: "Multi-account keychain operations"
      exports: ["saveAccount", "loadAccounts", "deleteAccount", "activeAccountId"]
    - path: "ZaiSubscriptionWidget/ViewModels/UsageViewModel.swift"
      provides: "Account management and switching"
      contains: "accounts: [Account]"
    - path: "ZaiSubscriptionWidget/Views/SettingsView.swift"
      provides: "Account list UI with add/remove"
    - path: "ZaiSubscriptionWidget/Views/MenuBarView.swift"
      provides: "Account selector dropdown"
  key_links:
    - from: "MenuBarView.swift"
      to: "UsageViewModel.accounts"
      via: "account picker binding"
    - from: "UsageViewModel.swift"
      to: "KeychainService"
      via: "loadAccounts/saveAccount"
    - from: "SettingsView.swift"
      to: "UsageViewModel"
      via: "addAccount/removeAccount methods"
---

<objective>
Add multi-account support to allow users to configure and switch between multiple Z.AI accounts.

Purpose: Enable users with multiple Z.AI accounts to monitor usage across all accounts without re-entering API keys.
Output: Working multi-account system with account management UI and switching capability.
</objective>

<execution_context>
@/Users/alexey/Projects/zai-subscripton-info/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/alexey/Projects/zai-subscripton-info/.opencode/get-shit-done/templates/summary.md
</execution_context>

<interfaces>
<!-- Key types from existing codebase -->

From KeychainService.swift:
```swift
class KeychainService {
    static let shared: KeychainService
    func saveAPIKey(_ apiKey: String) throws
    func loadAPIKey() -> String?
    func deleteAPIKey() throws
}
```

From UsageViewModel.swift:
```swift
@MainActor
class UsageViewModel: ObservableObject {
    static let shared: UsageViewModel
    @Published var apiKey: String
    @Published var quotaLimits: [QuotaLimitItem]
    @Published var isLoading: Bool
    @Published var error: String?
    
    var hasAPIKey: Bool { get }
    func refresh() async
    func saveAPIKey(_ key: String)
}
```

From ZaiAPIService.swift:
```swift
actor ZaiAPIService {
    static let shared: ZaiAPIService
    func fetchQuotaLimit(apiKey: String) async throws -> [QuotaLimitItem]
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Account model and update KeychainService</name>
  <files>
    ZaiSubscriptionWidget/Models/Account.swift,
    ZaiSubscriptionWidget/Services/KeychainService.swift
  </files>
  <action>
    1. Create `ZaiSubscriptionWidget/Models/Account.swift`:
       - `Account` struct with: `id: UUID`, `name: String`, `apiKey: String`
       - Conform to `Codable`, `Identifiable`, `Hashable`
       - Add computed property `obfuscatedKey` to show last 4 chars only for display
    
    2. Update `KeychainService.swift`:
       - Keep existing single-key methods for migration compatibility
       - Add `accountsKey = "zai-accounts"` constant for storing accounts JSON
       - Add `activeAccountKey = "zai-active-account-id"` for active account ID
       - Add `saveAccount(_ account: Account) throws` - saves account to array, updates keychain
       - Add `loadAccounts() -> [Account]` - loads all accounts from keychain
       - Add `deleteAccount(id: UUID) throws` - removes account from array
       - Add `saveActiveAccountId(_ id: UUID?)` and `loadActiveAccountId() -> UUID?`
       - Use JSON encoding/decoding for accounts array storage
       - Handle migration: if old single key exists but no accounts, create default account from it
  </action>
  <verify>
    <automated>xcodebuild -scheme ZaiSubscriptionWidget build 2>&1 | grep -E "(error:|BUILD SUCCEEDED)"</automated>
  </verify>
  <done>Account model exists, KeychainService has multi-account methods, builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Update UsageViewModel for account management</name>
  <files>ZaiSubscriptionWidget/ViewModels/UsageViewModel.swift</files>
  <action>
    Update `UsageViewModel.swift` to support multiple accounts:
    
    1. Add published properties:
       - `@Published var accounts: [Account] = []`
       - `@Published var activeAccount: Account?`
    
    2. Update `loadSavedSettings()`:
       - Load accounts via `KeychainService.shared.loadAccounts()`
       - Load active account ID via `KeychainService.shared.loadActiveAccountId()`
       - Set `activeAccount` to matching account or first account
       - Handle migration: if old apiKey exists but no accounts, create "Default" account
    
    3. Add account management methods:
       - `func addAccount(name: String, apiKey: String)` - creates and saves new account
       - `func removeAccount(_ account: Account)` - deletes account, switches to another if active
       - `func switchToAccount(_ account: Account)` - sets active account, refreshes data
       - `func updateAccount(_ account: Account)` - updates account name/key
    
    4. Update `refresh()` to use `activeAccount?.apiKey` instead of `apiKey` property
    
    5. Update `hasAPIKey` computed property to check `activeAccount != nil`
    
    6. Keep `apiKey` property for backward compatibility but deprecate its usage
  </action>
  <verify>
    <automated>xcodebuild -scheme ZaiSubscriptionWidget build 2>&1 | grep -E "(error:|BUILD SUCCEEDED)"</automated>
  </verify>
  <done>ViewModel manages multiple accounts, active account switching works, builds successfully</done>
</task>

<task type="auto">
  <name>Task 3: Update UI for account management</name>
  <files>
    ZaiSubscriptionWidget/Views/SettingsView.swift,
    ZaiSubscriptionWidget/Views/MenuBarView.swift
  </files>
  <action>
    1. Update `SettingsView.swift`:
       - Replace single API key field with account list section
       - Show list of accounts with name and obfuscated key (e.g., "Work ••••••••1234")
       - Add "+" button to show sheet for adding new account (name + API key fields)
       - Add swipe-to-delete or "-" button for removing accounts
       - Add edit functionality for account name
       - Show "Add your first account" placeholder when no accounts exist
       - Keep refresh settings section unchanged
    
    2. Update `MenuBarView.swift`:
       - Add account picker at top of content view (below title or in header)
       - Use `Picker` or custom dropdown showing account names
       - When account changes, call `viewModel.switchToAccount()`
       - Show current account name in header area
       - Hide picker when only one account exists (or show as subtle indicator)
       - Update `noAPIKeyView` to say "Add an account" instead of "API Key Required"
  </action>
  <verify>
    <automated>xcodebuild -scheme ZaiSubscriptionWidget build 2>&1 | grep -E "(error:|BUILD SUCCEEDED)"</automated>
  </verify>
  <done>Settings shows account list with add/remove, MenuBar has account selector, builds successfully</done>
</task>

</tasks>

<verification>
- Build succeeds with no errors
- Account model is Codable and Identifiable
- KeychainService supports multiple accounts with JSON storage
- ViewModel exposes accounts array and activeAccount
- SettingsView displays account list
- MenuBarView shows account selector
</verification>

<success_criteria>
- User can add multiple named accounts with API keys
- User can switch between accounts via menu bar dropdown
- Selected account's usage data is displayed
- User can delete accounts from settings
- Existing single-key users are migrated to "Default" account automatically
</success_criteria>

<output>
After completion, create `.planning/quick/260321-147-multi-account-support/260321-147-SUMMARY.md`
</output>
